
// adding a sting to a number results in the number being converted to a string. 
//this is called type coercion
"hello" + 3
//interpolation
// ${} using this combination of characters allows us to insert values directly into a string
//interpolation is a much cleaner way of adding items into a string
`items (${1+1}): $ ${2+1}`

// MULTI-LINE STRINGS
`some
texts`
// MULTI-LINE COMMENTS
/*  */